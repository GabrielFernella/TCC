export interface ScheduleInterface {
  id?: number;
  diasemana: string;
  entrada: string;
  saida: string;
}

export interface ILogin {
  id: number;
  name: string;
}

export interface IProfessorData {
  id: string;
  name: string;
  avatar: string;
  whatsapp: string;
}
